# 基数とn進数、基数変換

## 基になる数字
これが基数。例えば、2進数だったら、基数は2。

## 基数変換
### n進数 -> 10進数
- 各桁の重みを掛け合わせて合計
例えば、以下のような感じ

16進小数の0.Cを10進小数に変換

これは、まず、Cを10進数に変換して考えてみる。
16進数のCは10進数に直すと、12。

なので、今回は、0という整数と12という小数になる。
この小数部分がまだ10進小数に変換できていないから、これを桁の重み(1/16)で割る必要がある。

12 / 16 = 3 / 4 = 0.75  Ans. 0.5

### 10進数 -> n進数
- 基数で割って行く方法が割と簡単で便利
- 小数がある場合は、整数部分は基数で割り、小数は基数を1になるまで掛ける

### 8進数
- 3桁ずつ区切って計算（2^0、2^1、2^2)

### 16進数
- 4桁ずつ区切って計算（2^0、2^1、2^2、2^3)
